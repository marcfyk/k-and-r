int main() {
  /*
   * ### program's properties:
   * - counts the number of words.
   * - stops counting when EOF is read.
   * - words are a sequence of characters
   *   that are not space, tab, newline.
   *
   * ### test inputs
   * - empty input (just EOF).
   * - single character separated words,
   *   where these characters are either
   *   space, tab, newline.
   * - words that are separated with varying
   *   number of spaces, tabs, and newlines separating
   *   words.
   * - input with leading spaces, tabs and newlines.
   * - input with tailing spaces, tabs and newlines.
   */
}
